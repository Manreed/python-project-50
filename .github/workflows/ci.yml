name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: poetry install
      - name: Run flake8
        run: poetry run flake8 gendiff

